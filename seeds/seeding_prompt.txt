I am building a relational database for a professional German-learning application (A1-C1 level) geared towards Farsi speakers. 

I have attached a JSON file containing seed data for a specific category. This data uses a strict "Semantic Anchor" architecture. 

Your task is to act as a Senior Data Engineer and Linguist. You will analyze the attached JSON file and generate 250 completely NEW, highly relevant, and modern concepts for the SAME `category_slug`. 

You MUST obey these strict architectural rules:

1. The "Golden Rule of Concepts": A Concept represents a single, atomic mental image. If the Farsi translation would change, or if the nuance is distinctly different, you MUST create separate concept blocks. 
2. Strict Atomicity (NO PHRASES): Generate ONLY single words or reflexive verbs (e.g., "sich vorstellen"). Do NOT generate sentences, greetings, or multi-word phrases (e.g., No "Guten Morgen", No "Wie geht es dir", No "Ich bin"). Every `related_german_words` entry must be a dictionary-level headword.
3. `semantic_key`: Must be formatted as `category_slug-concept_name`. It must be unique and immutable.
4. `related_german_words` Array: 
   - The FIRST word in the array is the Primary Word.
   - Subsequent words MUST be EXACT SYNONYMS. 
   - Nouns must be capitalized (e.g., "Mensch"). Verbs/Adjectives must be lowercase (e.g., "menschlich").
5. Part of Speech Diversity: Provide a realistic mix of Nouns, Verbs, and Adjectives.
6. The "No Regurgitation" Rule: Do NOT output concepts already in the uploaded JSON file.
7. Output Format: Return ONLY a raw JSON array. No explanations. No markdown prose.